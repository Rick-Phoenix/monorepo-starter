$schema: "https://moonrepo.dev/schemas/tasks.json"

tasks:
  clean_cache:
    command: "rm -rf ./.cache"
    options:
      cache: false
  clean_dist:
    command: "rm -rf ./dist"
    options:
      cache: false
