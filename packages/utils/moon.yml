$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  barrel:
    command: "bun ./scripts/barrel.ts"
    inputs:
      - "src/**/*"
      - "scripts/barrel.ts"
    outputs:
      - "src/index.ts"
  build:
    deps:
      - "barrel"
  tsdown:
    deps:
      - "barrel"
