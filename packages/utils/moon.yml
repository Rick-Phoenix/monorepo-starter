$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  barrel:
    command: "bun ./scripts/barrel.ts"
    inputs:
      - "src/**/*"
    outputs:
      - "src/index.ts"
  build:
    deps:
      - barrel
