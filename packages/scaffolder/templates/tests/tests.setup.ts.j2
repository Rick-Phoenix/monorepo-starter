import { afterAll, beforeAll, vi } from "vitest";

beforeAll(() => {
  vi.mock("../src/lib/env", () => {
    return {
      default: {
        NODE_ENV: "test",
        PORT: 3000,
        DATABASE_URL: "",
        LOG_LEVEL: "info",
        GITHUB_CLIENT_ID: "",
        GITHUB_CLIENT_SECRET: "",
        GITHUB_CALLBACK_URI: "",
        SESSION_ENCRYPTION_KEY: "",
      },
    };
  });
});

afterAll(() => {});

